Description:
    Generates new set files in mods and matching specs. Generated in the deck (application) directory.

Example:
    wagn generate set 05_standard type tshirt

    This will create:
        mod/05_standard/set/type/tshirt.rb
        spec/mod/05_standard/set/type/tshirt_spec.rb
